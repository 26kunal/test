  # s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
            # ss = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
            # host=socket.gethostname()
           
            # port=5000
            # s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
            # s.bind((host,port))
            # s.listen(1)
            # print(host)
            # print("waiting for connection")
            # # client()
            # ss = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
            # # host = input(str("please enter host address: "))
            # # port=5000
            # ss.connect((socket.gethostname(),port))
            # conn, addr= s.accept()
            # print(addr, "has connected!")
            # # filename=input(str("enter filename"))
            # file=open(file_name.filename,'rb')
            # send_data=file.read(80000)
            # conn.send(send_data)
            # print("data is send")
            # recv_data=(ss.recv(80000))

            # filename="data.txt"
            # # if int(file_data[1])==48:
            # #     filename="data.txt"
            # # else:
            # #     filename="data.csv"
            # file=open(filename,'wb')

            # file.write(recv_data)
            # file.close()
            # print("recieved")